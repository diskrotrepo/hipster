name: hipster
description: "Hipster: A meta data analysis tool for the Suno platform."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'


dependencies:
  flutter:
    sdk: flutter

  font_awesome_flutter:  ^10.8.0
  get_it: ^8.0.3
  http: ^1.3.0
  audioplayers: ^6.5.0
  url_launcher: ^6.2.1
  shared_preferences: ^2.5.3
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/diskrot_logo.png
